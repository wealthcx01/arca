# library/ — venture artifacts + agent outputs (D8)

Agent and founder artifacts (decks-as-text, generated copy, reports). `library/runreports/` on the
`foundry-state` ref carries lane RunReports. Heavy binaries → object storage + a pointer file.