# context/ — the venture's durable brain (D8)

Founder-supplied background every lane can read: brand kit, ICP, positioning, pricing decisions,
research. Department-tagged: `context/build/`, `context/sell/`, `context/scale/`. Deposited from the
Foundry composer (FB-043) or committed directly. Git is the source of truth; the agent lanes read
this during their RESEARCH step, so what the founder tells the composer reaches the work.

No secrets here (a deposit is secret/PII-scanned). Heavy binaries live in object storage with a
pointer file, never in git.